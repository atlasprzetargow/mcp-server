node_modules/
dist/
*.log
.env
.env.local
.DS_Store
# src/generated/ is committed so the package builds without the Atlas monorepo.
# The prebuild script will regenerate/overwrite these files when run inside the monorepo.
